## Plan

本项目尝试实现一个迷你版的 Calcit 解释器, 用于 Respo 的事件监听器.
在解释器上下文中, 将注入

- `state` 对应组件的状态, 使用 JSON 结构,
- `dispatch!` 函数, 用于发送 Action, 也会用到 JSON 结构,

所以实际上是基于 JSON 的动态语言的解释器. 但沿用 Calcit 当中的语法.

可能会实现 Calcit 的 `quote` 指令, 以便代码能够更方便地多阶段执行.

当前项目的思路是想通过脚本代码, 把闭包逻辑表示出来, 在宿主 MoonBit 的运行环境当中使得闭包能够序列化使用.

### 计划实现

- 嵌套 JSON 结构的读写
- 访问 JavaScript 对象的属性等等
- 基本的四则运算
- 简单的字符串操作
- 函数调用
- 在 `context` 中注入函数补充其他功能
- 简单的闭包, 可能只是小部分功能
- `quote` 指令, 用于表示代码
